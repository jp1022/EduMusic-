<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".BeatsLevelActivity"
    android:background="#ffffffff">

    <!-- Notes -->
    <Button
        android:layout_width="60dp"
        android:layout_height="30dp"
        android:text="150"
        android:id="@+id/notes"
        android:layout_alignParentStart="true"
        android:background="@layout/notes_border"/>

    <!-- Text and Instructions -->

    <!-- Begin Level Button -->

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play Pitch"
        android:id="@+id/startPitch"
        android:onClick="playPitch"
        android:layout_above="@+id/choice_one"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="88dp"
        android:background="@layout/button_border"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="One"
        android:id="@+id/choice_one"
        android:onClick="buttonOne"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:background="@layout/button_border"
        android:layout_marginBottom="30dp"
        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Two"
        android:id="@+id/choice_two"
        android:onClick="buttonTwo"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@layout/button_border"
        android:layout_marginBottom="30dp"

        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Which is higher?"
        android:id="@+id/instruction"
        android:textSize="40sp"
        android:layout_above="@+id/startPitch"
        android:layout_centerInParent="true"
        android:layout_marginBottom="110dp"
        android:gravity="center"/>


</RelativeLayout>